---
'@lorion-org/react': minor
'@lorion-org/nuxt': minor
---

Add opt-in composition-bound contribution loading with exact physical source identity checks.
React exposes an explicit provider and typed collection hook; Nuxt generates a per-application
runtime plugin and typed native composable. Existing contribution and registry APIs remain
unchanged. Both playgrounds demonstrate selected shop versions, provider-owned checkout and
optional native UI contributions.
